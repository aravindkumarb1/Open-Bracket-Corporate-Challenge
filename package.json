{
  "name": "bcus-christiana-vr",
  "version": "1.0.0",
  "description": "Christiana Care Patient Relaxation",
  "main": "index.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha --reporter spec",
    "build": "mkdir -p ./dist && browserify index.js -o dist/index.js -t [ babelify --presets [ es2015 ] ]",
    "serve": "npm run build && node server.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babelify": "^7.3.0",
    "browserify": "^13.1.1",
    "chai": "^3.5.0",
    "express": "^4.14.0",
    "mocha": "^3.1.2"
  },
  "dependencies": {
    "three-js": "^79.0.0"
  }
}
